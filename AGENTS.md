# AGENTS.md — Root Entrypoint

> Auto-loaded by Claude Code, Codex, and other agents that read a root-level
> `AGENTS.md`/`CLAUDE.md`. GitHub Copilot auto-loads
> `.github/copilot-instructions.md` instead. To avoid duplication drift, this
> file is a **pointer, not a manual** — the single source of truth for how to
> operate here is the initializer prompt.

**Read first, in order:**

1. `.github/copilot-instructions.md` — how to operate: session checklist, golden rules, working loop, definition of done, skill registry
2. `docs/AGENTS.md` — repo map, build commands, conventions
3. `docs/LESSONS.md` — lesson index, session history, memory maintenance
4. `docs/architecture.md` — system-level spec

Then follow the **Session Start Checklist** in `.github/copilot-instructions.md` exactly.
